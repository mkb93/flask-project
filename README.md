# Item tracker
this application tracks where items are in my house

the mvp is deployed 
https://flask-project-git-sort-errors-marco-belahouanes-projects.vercel.app/
 note: the backend takes awhile to load
as you can see vercel host the frontend, render hosts the backend and the database is hosted on supabase

Latest Update:
 Due to the lack of use of supabase, the database has been discarded. so only the frontend works.  :(

